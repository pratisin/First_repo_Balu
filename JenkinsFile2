pipeline {
   agent any
   tools {
        maven 'Maven'

    }
   stages {
   
        stage('Build') {
            steps {
               snDevOpsStep ()
                sh 'mvn -B -DskipTests clean compile'
            }
        }
        
		stage('Deploy') {

            steps {
                //snDevOpsChange()
				snDevOpsStep ()
                echo 'Executing deploy'

            }
      }
      
  }
}
